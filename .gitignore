/target
/.smoke-test-data
**/*.rs.bk
*.checkpoint.json
*.checkpoint.json.tmp
.DS_Store

# Go build artifacts: OCB regenerates these from otelcol/builder-config.yaml
# (main.go, go.mod, go.sum, the compiled binary itself).
/otelcol/dist/
/otelcol/.smoke-test/

# sgcia-fleet-server's compiled binary and its runtime SQLite inventory.
/fleet/sgcia-fleet-server/sgcia-fleet-server
/fleet/sgcia-fleet-server/*.db
